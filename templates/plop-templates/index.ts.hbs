import { {{ className name }} } from './{{> tagnamePartial }}.element';
import { defineElement } from '@umbraco-ui/uui-base/lib/registration';

defineElement('{{> tagnamePartial }}', {{ className name }} as any);
