{
  "name": "@umbraco-ui/uui-base",
  "version": "0.0.1",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "lib"
  ],
  "dependencies": {
    "lit": "^2.0.0-rc.3"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build && rollup -c rollup.config.js"
  }
}
