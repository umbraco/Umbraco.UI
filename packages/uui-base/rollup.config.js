import { UUIProdConfig } from '../rollup-component.config';

export default UUIProdConfig({
  entryPoints: ['animations', 'events', 'mixins', 'types', 'uui-boolean-input/uui-boolean-input-base.element'],
});
