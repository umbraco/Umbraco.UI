# uui-symbol-drag-handle

![npm](https://img.shields.io/npm/v/@umbraco-ui/uui-symbol-drag-handle?logoColor=%231B264F)

Umbraco style drag-handle component.

## Installation

### ES imports

```zsh
npm i @umbraco-ui/uui-symbol-drag-handle
```

Import the registration of `<uui-symbol-drag-handle>` via:

```javascript
import '@umbraco-ui/uui-symbol-drag-handle';
```

When looking to leverage the `UUIDragHandleElement` base class as a type and/or for extension purposes, do so via:

```javascript
import { UUIDragHandleElement } from '@umbraco-ui/uui-symbol-drag-handle';
```

## Usage

```html
<uui-symbol-drag-handle></uui-symbol-drag-handle>
```
