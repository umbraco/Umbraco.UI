import { LitElement, html, css } from 'lit';

/**
 * @element uui-tabs
 */
export class UUITabsElement extends LitElement {
  static styles = [
    css`
      :host {
        /* Styles goes here */
      }
    `,
  ];

  render() {
    return html` Markup goes here `;
  }
}
