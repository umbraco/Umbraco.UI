{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "lib/**/*.js",
        "lib/**/*.d.ts",
        "dist/**",
        "custom-elements.json"
      ]
    },
    "test": {},
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  },
  "baseBranch": "origin/v1/contrib",
  "globalDependencies": ["tsconfig.json"]
}
