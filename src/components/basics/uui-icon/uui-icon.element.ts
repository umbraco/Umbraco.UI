import { LitElement, html, css, property } from 'lit-element';
import {
  SymbolicColor,
  SymbolicColorDefaultValue,
} from '../../../type/SymbolicColor';

/**
 *  @element uui-icon
 *
 */

export class UUIIconElement extends LitElement {
  static styles = [css``];

  render() {
    return html` <p>Girl, you'll be an icon soon</p> `;
  }
}
