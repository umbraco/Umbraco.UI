import { LitElement, html, css, property } from 'lit-element';

/**
 *  @element uui-list-item
 *
 */

export class UUIListItemElement extends LitElement {
  static styles = [css``];

  render() {
    return html` <p>hello i'll be a list item</p> `;
  }
}
