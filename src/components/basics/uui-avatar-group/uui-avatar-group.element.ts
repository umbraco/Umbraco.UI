import { LitElement, html, css } from 'lit-element';

/**
 *  @element uui-avatar-group
 */

export class UUIAvatarGroupElement extends LitElement {
  static styles = [css``];

  render() {
    return html`<div>UUI AVATAR GROUP</div>`;
  }
}
