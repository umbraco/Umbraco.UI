import { html } from 'lit-html';
import './index';

export default {
  title: 'Layout/Lead',
  component: 'uui-lead',
};

export const Default = () =>
  html` <uui-lead>Some lead paragraph text</uui-lead>`;
