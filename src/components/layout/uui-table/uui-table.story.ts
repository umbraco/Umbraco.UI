import { html } from 'lit-html';
import './index';

export default {
  title: 'Layout/Table',
  component: 'uui-table',
};

export const Default = () =>
  html` <uui-table>Some lead paragraph text</uui-table>`;
